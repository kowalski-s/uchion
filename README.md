# AI Worksheet Generator

Веб-приложение для автоматической генерации рабочих листов по школьным предметам (1–4 классы).  
Сервис создаёт:  
- краткий конспект,
- 3–5 заданий разного формата,
- блок вопросов,
- PDF-файл для скачивания.

Основано на OpenAI (gpt-5-mini + gpt-4.1-mini) и собственном backend API.

---

## 🚀 Возможности

- Генерация рабочих листов по теме урока.
- Поддержка предметов: **математика**, **русский язык**.
- Поддержка классов: **1–4**.
- Строго структурированный формат JSON → PDF.
- Быстрое и минималистичное SPA-приложение.
- PDF генерируется на сервере «на лету».
- Полностью защищённая работа с API-ключами.

---

## 🧩 Архитектура

Проект состоит из:

### **Frontend**
- React + TypeScript  
- Vite  
- Tailwind CSS + shadcn/ui  
- React Hook Form + Zod  
- React Query  

### **Backend**
- Vercel Serverless Functions (Node.js + TypeScript)  
- Endpoint `/api/generate`  
- Модуль `AIProvider` для взаимодействия с OpenAI  
- PDF-генерация через pdfkit  

### **AI**
- Generation: OpenAI (gpt-5-mini)
- Validation: OpenAI (gpt-4.1-mini)
- OpenRouter (возможность легко переключать модели)

Полная архитектура → см. `/docs` (architecture-overview, tech-stack, api-design и т.д.)

---

## 📁 Структура проекта

/docs # документация проекта
architecture-overview.md
tech-stack.md
api-design.md
security-and-performance.md
ux-ui-guidelines.md
scaling-plan.md
prompts-ai.md

/src
/components # UI-компоненты
/pages or /routes # страницы SPA
/features # логические блоки
/lib # утилиты, валидации, API-клиенты
/api # фронтовой клиент для бэкенда
main.tsx

/api
generate.ts # backend endpoint для генерации

/public # статика

README.md
.gitignore


---

## ⚙️ Подготовка окружения

### 1. Установка зависимостей:
```bash
npm install
```

### 2. Создание файла окружения:

Создай `.env.local` для локальной разработки:

```bash
# API Key (не обязателен для локальной разработки с заглушкой)
OPENAI_API_KEY=ваш_ключ

# Выбор провайдера: 'dummy' (по умолчанию без ключа) или 'openai'
AI_PROVIDER=dummy
```

Для продакшена (Vercel) задайте переменные в настройках проекта:
- `OPENAI_API_KEY`
- `AI_PROVIDER=openai`

3. Запуск проекта
npm run dev

🛠 Принципы безопасности

API-ключи в ENV только на backend.

CORS ограничен доменом фронтенда.

Валидация входящих данных (Zod).

JSON-ответ ИИ проверяется строгой схемой.

PDF генерируется на сервере — безопасно.

Нет персональных данных в MVP.

Полная модель безопасности → /docs/security-and-performance.md.

🔮 Roadmap

Кратко:

MVP (генерация + PDF)

Улучшение качества материалов

Личный кабинет

База данных

Монетизация

Переход на VPS

Расширение предметов

Полная дорожная карта → /docs/roadmap.md.

🧪 Тестирование

Unit: Vitest/Jest

Integration: Supertest

E2E: Playwright/Cypress

Тестовые промпты → /docs/prompts-ai.md.