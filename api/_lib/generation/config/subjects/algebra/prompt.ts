import type { SubjectPromptConfig, DifficultyLevel } from '../../types.js'

function contentRequirements(grade: number, level: DifficultyLevel): string {
  if (grade >= 9) {
    return `
СОДЕРЖАНИЕ ЗАДАНИЙ (алгебра, ${grade} класс):
- Задания содержат конкретные уравнения, выражения или функции для работы
- Текстовые задачи должны быть на составление уравнений/систем с реальным контекстом
- Для тестов: дистракторы отражают типичные ошибки (потеря корня, неправильная ОДЗ, ошибка знака)
- Включай задания, требующие НЕСКОЛЬКИХ шагов: упростить \u2192 решить \u2192 проверить`
  }
  return `
СОДЕРЖАНИЕ ЗАДАНИЙ (алгебра, ${grade} класс):
- Задания содержат конкретные уравнения, выражения или функции
- Дистракторы в тестах отражают типичные ошибки учеников`
}

export const algebraPromptConfig: SubjectPromptConfig = {
  systemPrompt: `ПРЕДМЕТ: Алгебра (7-11 класс)

Особенности:
- Строгая математическая запись
- Уравнения и выражения записывать корректно
- Ответы могут быть числами, выражениями или множествами
- Использовать стандартные обозначения (x, y, a, b)`,

  contentRequirements,

  diversityHints: `
Для математических предметов обязательно включи:
\u2022 Задания на знание ФОРМУЛ (запиши формулу, выбери правильную формулу)
\u2022 Задания на ПОНИМАНИЕ (что изменится если..., верно ли что..., объясни)
\u2022 Задания на ВЫЧИСЛЕНИЕ (реши, найди, вычисли)
\u2022 Задания на АНАЛИЗ (найди ошибку, сравни способы решения, верно ли утверждение)`,
}
